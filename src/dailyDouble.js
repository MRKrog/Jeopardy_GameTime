class DailyDouble extends Clues {
  constructor(question, pointValue, answer, categoryId, dailyDble) {
    super(question, pointValue, answer, categoryId, dailyDble);
    this.wager = wager;
  }

  assignDailyDouble() {

  }
}

export default DailyDouble;
